specVersion: "v1beta"
serviceId: "ncw-event-organizer"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ncw-event-organizer-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
